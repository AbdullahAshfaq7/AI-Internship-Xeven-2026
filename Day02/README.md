# Day 02 — Python Variables & Data Types

## What I did today
- Explored Python's 4 main data types: int, float, bool, str
- Practiced type conversion between different types
- Built an interactive calculator with error handling

## Files
- `data_types.py` — demonstrates all 4 data types with type() and conversion
- `calculator.py` — interactive calculator with 4 operations and try/except

## What I learned
- `input()` always returns a string — always convert it
- `try/except` prevents crashes from invalid user input
- PEP 8 naming: use snake_case for all variable names

## How to run
python3 data_types.py
python3 calculator.py

## Emailed Progress Report To Team Lead.